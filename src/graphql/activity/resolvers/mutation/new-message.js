// const { Notification } = require('../../../../models');

// const newMessage = async (root, args, ctx) => {
//   // console.log('newMessageMutation', args, ctx);
//   const activity = await ctx.models.Activity.getActivityDetails(args);

//   Notification.createNotification({

//   });

//   return activity;
// };

// module.exports = newMessage;
